
set_polygon_mode 0			// 0 - fill, 1 - line, 2 - point

set_max_shadow_lights 2			// maximum shadow lights, 1 pass per 1 shadow light

set_fog_color 0.5 1 0.5			// fog color, affects all fog mode

set_fog_start 0				// fog start distance, affects only linear fog mode

set_fog_end 1000			// fog end distance, distance beyond this also receives full fog, affects only linear fog

set_fog_density 0.0025			// fog density, only affects exp and exp2 fog mode

set_glow_steps 3			// glow steps, the horizontal and vertical blur size

set_clear_color 0.5 0.5 0.5